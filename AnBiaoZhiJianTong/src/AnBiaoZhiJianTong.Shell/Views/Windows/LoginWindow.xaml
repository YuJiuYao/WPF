<Window x:Class="AnBiaoZhiJianTong.Shell.Views.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:viewModels="clr-namespace:AnBiaoZhiJianTong.Shell.ViewModels.Windows"
        d:DataContext="{d:DesignInstance Type=viewModels:LoginWindowViewModel}"
        mc:Ignorable="d"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="验证码登录"
        Width="380"
        Height="360"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False">
    <Border CornerRadius="10"
            Background="White"
            Padding="0"
            SnapsToDevicePixels="True">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12"
                              ShadowDepth="0"
                              Opacity="0.2"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"
                  Background="#F5F7FF"
                  MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="验证码登录"
                           VerticalAlignment="Center"
                           Margin="16,0,0,0"
                           FontSize="14"
                           FontWeight="Bold"
                           Foreground="#1F2430"/>

                <Button Grid.Column="1"
                        Width="32"
                        Height="32"
                        Margin="0"
                        BorderThickness="0"
                        Background="Transparent"
                        Foreground="#6F7A8C"
                        FontSize="14"
                        FontWeight="Bold"
                        Content="×"
                        Cursor="Hand"
                        Click="CloseButton_Click"/>
            </Grid>

            <StackPanel Grid.Row="1"
                        Margin="24,20,24,20"
                        VerticalAlignment="Stretch"
                        HorizontalAlignment="Stretch">
                <TextBlock Text="使用手机号接收验证码并登录"
                           FontSize="12"
                           Foreground="#6F7A8C"
                           Margin="0,0,0,16"/>

                <hc:WatermarkTextBox x:Name="PhoneTextBox"
                                     Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"
                                     Watermark="请输入手机号"
                                     MaxLength="11"
                                     Height="36"
                                     Padding="10,0"
                                     FontSize="13"
                                     PreviewTextInput="PhoneTextBox_PreviewTextInput"
                                     DataObject.Pasting="PhoneTextBox_OnPasting"
                                     Margin="0,0,0,16"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="120"/>
                    </Grid.ColumnDefinitions>

                    <hc:WatermarkTextBox Grid.Column="0"
                                         Text="{Binding VerificationCode, UpdateSourceTrigger=PropertyChanged}"
                                         Watermark="请输入验证码"
                                         Height="36"
                                         Padding="10,0"
                                         FontSize="13"/>

                    <Button Grid.Column="1"
                            Margin="12,0,0,0"
                            Height="36"
                            MinWidth="108"
                            Content="{Binding RequestCodeButtonText}"
                            Command="{Binding RequestCodeCommand}"
                            Cursor="Hand"
                            Background="#2D6BF3"
                            BorderBrush="#2D6BF3"
                            Foreground="White"
                            FontSize="12"
                            BorderThickness="0"
                            HorizontalAlignment="Stretch"/>
                </Grid>

                <CheckBox Content="自动登录"
                          IsChecked="{Binding IsAutoLogin}"
                          Foreground="#4C5667"
                          Margin="0,16,0,0"/>

                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="{Binding StatusBrush}"
                           FontSize="12"
                           Margin="0,12,0,0"/>

                <Button Content="登录/注册"
                        Height="40"
                        Command="{Binding LoginCommand}"
                        Background="#2D6BF3"
                        BorderBrush="#2D6BF3"
                        Foreground="White"
                        FontSize="14"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand"
                        Margin="0,8,0,0"/>

                <TextBlock TextWrapping="Wrap"
                           FontSize="12"
                           Foreground="#6F7A8C"
                           HorizontalAlignment="Center"
                           Margin="0,16,0,0">
                    <TextBlock.Inlines>
                        <Run Text="未注册手机号验证后将自动注册并登录即代表同意"/>
                        <Hyperlink NavigateUri="{Binding UserServiceUrl}" RequestNavigate="Hyperlink_RequestNavigate" TextDecorations="{x:Null}">
                            <Run Text="《用户协议》" Foreground="#FF2196F3" Cursor="Hand" FontSize="12"/>
                        </Hyperlink>
                        <Run Text="及"/>
                        <Hyperlink NavigateUri="{Binding SecretServiceUrl}" RequestNavigate="Hyperlink_RequestNavigate" TextDecorations="{x:Null}">
                            <Run Text="《隐私协议》" Foreground="#FF2196F3" Cursor="Hand" FontSize="12"/>
                        </Hyperlink>
                    </TextBlock.Inlines>
                </TextBlock>
            </StackPanel>
        </Grid>
    </Border>
</Window>
