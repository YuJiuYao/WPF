<Window x:Class="AnBiaoZhiJianTong.Shell.Views.Windows.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:AnBiaoZhiJianTong.Shell.ViewModels"
        xmlns:windows="clr-namespace:AnBiaoZhiJianTong.Shell.ViewModels.Windows"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=windows:UpdateWindowViewModel}"
        Title="发现新版本"
        Height="400" Width="800"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        Background="Transparent">

    <!-- 关闭原生标题栏，保留 5px 的可缩放边（如不需要缩放可设为 0） -->
    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="0"
            ResizeBorderThickness="5"
            GlassFrameThickness="5"
            UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- 标题栏按钮：透明底、白字，Hover 变深灰 -->
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#34495E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 关闭按钮：Hover 变红 -->
        <Style x:Key="TitleBarCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E74C3C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <SolidColorBrush x:Key="Primary500" Color="#2196F3"/>
        <SolidColorBrush x:Key="Primary600" Color="#1976D2"/>
        <SolidColorBrush x:Key="Primary700" Color="#1565C0"/>
        <SolidColorBrush x:Key="TextOnPrimary" Color="#FFFFFF"/>

        <Style x:Key="BaseRoundedButton" TargetType="Button">
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="root"
                CornerRadius="3"
                BorderThickness="{TemplateBinding BorderThickness}"
                BorderBrush="{TemplateBinding BorderBrush}"
                Background="{TemplateBinding Background}"
                SnapsToDevicePixels="True">
                            <ContentPresenter
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- 键盘焦点虚线框：可视但不抢颜色 -->
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="root" Property="BorderBrush" Value="{StaticResource Primary500}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="root" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseRoundedButton}">
            <Setter Property="Background" Value="{StaticResource Primary500}"/>
            <Setter Property="Foreground" Value="{StaticResource TextOnPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Primary500}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource Primary600}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource Primary600}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource Primary700}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource Primary700}"/>
                </Trigger>
                <!-- 默认按钮：对话框回车键 -->
                <Trigger Property="IsDefault" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource Primary700}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- 外层：白底、圆角、细边、轻阴影 -->
    <Border CornerRadius="6"
            Background="White"
            BorderBrush="#DDDDDD"
            BorderThickness="1"
            SnapsToDevicePixels="True">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.18"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 自定义标题栏（与主窗体一致） -->
            <Grid Grid.Row="0"  Background="{StaticResource DarkPrimaryBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 左：图标 + 标题 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                    <TextBlock Text="暗标检查工具 - 更新" FontSize="13" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>

            </Grid>

            <!-- 内容区：按示意图重排 -->
            <Grid Grid.Row="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 行1：大标题 -->
                <TextBlock Grid.Row="0" Text="暗标检查工具" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>

                <!-- 行2：版本信息（左边徽牌：旧版 ====> 新版；右侧“当前版本号 / 升级至”对齐） -->
                <Grid Grid.Row="1">
                    <!-- 右侧：当前版本号 / 升级至 -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal"
                    HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Text="{Binding CurrentVersion}" FontSize="14" FontWeight="Bold" Margin="10,0,5,0"/>
                        <TextBlock Text="==>" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding LatestVersion}" FontSize="14" FontWeight="Bold" Foreground="#2C7BE5" Margin="5,0,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- 行3：更新说明框（可滚动） -->
                <Border Grid.Row="2" BorderBrush="#EAEAEA" BorderThickness="1" CornerRadius="6" Padding="12" Margin="10,10,0,10">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Height="120">
                        <TextBlock Text="{Binding UpgradeDescription, TargetNullValue=暂无更新说明}"
                       TextWrapping="Wrap" FontSize="13"/>
                    </ScrollViewer>
                </Border>

                <!-- 行4：进度条 + 居中百分比 -->
                <StackPanel Grid.Row="3" Orientation="Vertical" Margin="10,10,0,0">
                    <TextBlock Text="{Binding StatusText}" FontSize="14" Margin="0,0,0,8"/>
                    <ProgressBar Height="30"
                                 Minimum="0"
                                 Maximum="100"
                                 Value="{Binding ProgressValue}"
                                 IsIndeterminate="{Binding IsIndeterminate}"/>
                    <TextBlock Text="{Binding SpeedText}" FontSize="12" Foreground="Gray" Margin="0,6,0,0"/>
                </StackPanel>

                <!-- 行5：按钮 -->
                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="开始安装" Width="100" Height="32" Margin="0,0,10,0" 
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding InstallCommand}" 
                            IsEnabled="{Binding IsInstallEnabled}"/>
                    <Button Content="取消" Width="80" Height="32" 
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding CancelCommand}"/>
                </StackPanel>
            </Grid>

        </Grid>
    </Border>
</Window>
